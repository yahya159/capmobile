{
  "name": "field-task-cap",
  "version": "1.0.0",
  "dependencies": {
    "@cap-js/hana": "^2",
    "@sap/cds": "^9",
    "@sap/xssec": "^4.13.0"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2",
    "@sap/cds-dk": "^9"
  },
    "cds": {
    "requires": {
      "auth": {
        "kind": "mocked"
      }
    }
  }
,
  "scripts": {
    "start": "cds-serve"
  },
  "private": true
}
